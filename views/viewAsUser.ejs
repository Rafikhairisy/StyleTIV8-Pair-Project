<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <style>
        .card-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: flex-start;
            gap: 20px;
        }

        .card {
            width: 200px;
        }

        .card-img-top {
            width: 100%;
            height: auto;
        }
    </style>
</head>
<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand" href="/home">Styletiv8</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/home">All Products</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Categories</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/logout" style="color: green;">Logout</a>
                </li>
            </ul>
            <a href="/homeadmin"><button class="me-3">Back As Admin</button></a>
            <a href="/cart"><button><i class="fa fa-shopping-cart"></i></button></a>
            <form class="d-flex" role="search" action="/home" method="GET">
                <input class="form-control me-2" type="search" name="searchProduct" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-success" type="submit">Search</button>
            </form>
            
        </div>
    </div>
</nav>

<body>
    <div class="container">
        <h1>Styletiv8</h1>
        <div class="card-container">
            <% dataProduct.forEach((el) => { %>
            <div class="card">
                <img src="<%= el.image %>" class="card-img-top" alt="Product Image">
                <div class="card-body">
                    <h5 class="card-title"><%= el.productName %></h5>
                    <p class="card-text"><%= el.description %></p>
                    <p class="card-price"><%= Helper.formatRp(el.price) %></p>
                    <a href="#" class="btn btn-primary">Add to Cart</a>
                </div>
            </div>
            <% }) %>
    </div>
</body>


</html>